{
  "name": "cypressconfi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "local": "npx cypress run --headed --browser edge --config baseUrl=https://google.com",
    "staging": "npx cypress run --headed --browser edge --config baseUrl=https://facebook.com",
    "product": "npx cypress run --headed --browser edge --config baseUrl=https://yahoo.com"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "9.7"
  }
}